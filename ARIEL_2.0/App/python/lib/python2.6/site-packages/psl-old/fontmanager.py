
from psl import *

import os

class FontManager:

    def __init__(self):
        self.fonts = {}

    def getFont(self, face, size):
#        print "GF",os.getcwd()
        try:
            return self.fonts[(face,size)]
        except:
            font = ofTrueTypeFont()
            font.loadFont(face,size)
            #print "font load test",font.bLoadedOk
            if not font.isLoaded():
                print "failed to load",os.getcwd(),face
                fail
            self.fonts[(face,size)] = font
            return self.fonts[(face,size)]

manager = FontManager()

def getFont(face, size):
    global manager
    return manager.getFont(face, size)
